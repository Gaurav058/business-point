const DOMAIN="businesspoint.live";const API="https://api.godaddy.com/v1";const RECORDS=[{type:"A",name:"@",data:"76.76.21.21",ttl:600},{type:"CNAME",name:"www",data:"cname.vercel-dns.com",ttl:600}];async function main(){const key=process.env.GODADDY_KEY;const secret=process.env.GODADDY_SECRET;if(!key||!secret){console.error("ERROR: Set GODADDY_KEY and GODADDY_SECRET environment variables");console.error("Get keys at: https://developer.godaddy.com/keys");process.exit(1);}const auth=`sso-key ${key}:${secret}`;console.log("Applying Vercel DNS records for "+DOMAIN+"...");for(const r of RECORDS){const res=await fetch(`${API}/domains/${DOMAIN}/records/${r.type}/${r.name}`,{method:"PUT",headers:{Authorization:auth,"Content-Type":"application/json"},body:JSON.stringify([{data:r.data,ttl:r.ttl}])});if(res.ok){console.log("  OK  "+r.type+" "+r.name+" -> "+r.data);}else{const b=await res.text();console.error("  FAIL "+r.type+" "+r.name+": "+b);}}console.log("Done! DNS propagates in 10-30 minutes.");console.log("Run: node scripts/verify-dns.js to check");}main().catch(e=>{console.error(e.message);process.exit(1);});